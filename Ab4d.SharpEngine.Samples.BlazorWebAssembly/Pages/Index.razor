@page "/"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Navigate to the Common samples page on app start
        //Navigation.NavigateTo("/commonSamples", replace: true);
        Navigation.NavigateTo("/quickStart", replace: true);
    }
}