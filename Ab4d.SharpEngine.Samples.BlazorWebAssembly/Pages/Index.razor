@page "/"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Navigate to the Common samples page on app start
        //Navigation.NavigateTo("/commonSamples/Lights.LightsSample", replace: true);
        Navigation.NavigateTo("/introduction", replace: true);
    }
}